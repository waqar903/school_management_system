export const config = {
    db: {
        /* don't expose password or any sensitive info. */
        host: "localhost",
        user: "root",
        password: "",
        database: "sms",
    },
    listPerPage: 10,
};